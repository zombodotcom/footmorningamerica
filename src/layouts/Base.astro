---
import Footer from '../components/Footer.astro';
import AgeGate from '../components/AgeGate.astro';
import '../styles/global.css';
const { title = 'Foot Morning America', gated = false } = Astro.props;
const path = Astro.url.pathname;
const sections = [
  { href: '/', label: 'Front Page' },
  { href: '/news', label: 'Foot News' },
  { href: '/directory', label: 'The Directory' },
  { href: '/get-featured', label: 'Get Featured' },
];
const isActive = (href: string) => (href === '/' ? path === '/' : path.startsWith(href));
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Anton&family=Spectral:ital,wght@0,400;0,600;0,800;1,400&family=Special+Elite&family=UnifrakturCook:wght@700&display=swap"
      rel="stylesheet"
    />
    <title>{title}</title>
  </head>
  <body class="min-h-screen">
    <header class="mx-auto max-w-6xl px-4 pt-4">
      <!-- Masthead -->
      <div class="grid grid-cols-3 items-end gap-2 pb-2">
        <p class="dateline text-[10px] leading-tight sm:text-xs">
          Vol. I — No. 1<br />Weekend Edition<br />Est. This Morning
        </p>
        <p style="font-family: var(--font-masthead)" class="text-center text-3xl leading-none sm:text-5xl">
          <a href="/">Foot Morning America</a>
        </p>
        <p class="dateline text-right text-[10px] leading-tight sm:text-xs">
          Price: Free<br />Forecast: Toes Out<br />Weather: Humid
        </p>
      </div>
      <div class="rule-double"></div>
      <!-- Section index -->
      <nav class="flex flex-wrap items-center justify-center gap-x-5 gap-y-1 py-2 text-xs sm:text-sm" aria-label="Sections">
        {sections.map((s) => (
          <a
            href={s.href}
            class:list={[
              'dateline transition-colors hover:text-red',
              isActive(s.href) ? 'text-red underline underline-offset-4' : 'text-ink',
            ]}
            aria-current={isActive(s.href) ? 'page' : undefined}
          >{s.label}</a>
        ))}
      </nav>
      <div class="rule-thin"></div>
    </header>

    <main class="mx-auto max-w-6xl px-4"><slot /></main>

    <Footer />
    {gated && <AgeGate />}
  </body>
</html>
